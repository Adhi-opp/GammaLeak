"""core — engine-wide state, dataclass models, and configuration constants.

Single source of truth for things that must be imported from many places.
No business logic lives here; just shared types, state containers, and tunables.
"""
